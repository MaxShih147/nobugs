version: '3.8'

services:
  nobugs:
    build: .
    container_name: nobugs
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - VITE_DATA_SOURCE=notion
    restart: unless-stopped
